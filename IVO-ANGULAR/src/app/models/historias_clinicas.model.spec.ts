import { historias_clinicas } from "./historias_clinicas.model";

describe('historias_clinicas', () => {
  it('should create an instance', () => {
    expect(new historias_clinicas()).toBeTruthy();
  });
});
